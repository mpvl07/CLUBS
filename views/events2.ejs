<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        * {
  margin: 0;
  padding: 0;
  font-family: 'Montserrat', sans-serif;
}

.container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 50px;
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
}

.card {
  width: 30%;
  margin-bottom: 40px;
  background-color: #fff;
  border-radius: 10px;
  box-shadow: 0px 2px 10px rgba(0, 0, 0, 0.2);
  transition: transform .2s;
  overflow: hidden;
}

.card:hover {
  transform: translateY(-10px);
  box-shadow: 0px 5px 15px rgba(0, 0, 0, 0.3);
}

.card-image {
  position: relative;
  height: 200px;
  overflow: hidden;
}

.card-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform .2s;
}

.card-image:hover img {
  transform: scale(1.1);
}

.card-content {
  padding: 20px;
}

.card-title {
  font-size: 1.2rem;
  font-weight: bold;
  margin-bottom: 10px;
  color: #333;
}

.card-text {
  font-size: 0.9rem;
  line-height: 1.4em;
  color: #777;
  margin-bottom: 20px;
}

.card-btn {
  display: block;
  padding: 10px 20px;
  background-color: #333;
  color: #fff;
  border-radius: 25px;
  text-align: center;
  font-size: 0.9rem;
  text-transform: uppercase;
  font-weight: bold;
  letter-spacing: 1px;
  transition: background-color .2s;
  cursor: pointer;
}

.card-btn:hover {
  background-color: #222;
}

@media screen and (max-width: 992px) {
  .container {
    padding: 50px 30px;
  }
  
  .card {
    width: 48%;
  }
}

@media screen and (max-width: 768px) {
  .card {
    width: 100%;
  }
}

    </style>
</head>
<body>
    <div class="container">
        <% events.forEach(function(event) { %>
            <div class="card">
                <div class="card-image">
                    <img src="<%=event.image %>" alt="event-image">
                </div>
                <div class="card-content">
                    <h4 class="card-title">
                        <%= event.clubname %>
                    </h4>
                    <p class="card-text">
                        <%=event.title %>
                    </p>
                    <button class="card-btn"><a
                            href="/readmore?image=<%= event.image %>&clubname=<%= event.clubname %>&venue=<%= event.venue %>&date=<%= event.date %>&title=<%= event.title %>&description=<%= event.description %>&time1=<%= event.time1 %>">Read
                            More</a></button>
                </div>
            </div>
            <% }); %>
    </div>

</body>
</html>